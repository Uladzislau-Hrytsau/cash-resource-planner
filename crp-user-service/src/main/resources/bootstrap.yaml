spring:
  application:
    name: crp-user-service
  cloud:
    config:
      uri: http://127.0.0.1:8888
      retry:
        initial-interval: 1500
        multiplier: 1.5
        max-attempts: 10000
        max-interval: 1000

debug: true